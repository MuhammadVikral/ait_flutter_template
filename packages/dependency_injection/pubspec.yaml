name: dependency_injection
description: A new Flutter package project.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=2.18.4 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  common_dependency:
    path: "../../utils/common_dependency"
  auth:
    path: "../auth"
  navigation:
    path: "../../utils/navigation"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
